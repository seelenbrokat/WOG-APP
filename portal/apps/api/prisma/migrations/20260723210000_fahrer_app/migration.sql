-- Placeholder: Migration wurde auf der Live-DB bereits angewendet
-- (Eintrag in _prisma_migrations), fehlte aber im Repo.
-- Leerer Stub, damit prisma migrate history wieder konsistent ist.
SELECT 1;
